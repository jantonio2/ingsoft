<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="bo.ucb.edu.ingsoft.dao.DriverDao">
    <insert id="createAdministrationAccount" parameterType="bo.ucb.edu.ingsoft.modelo.Administration">
        INSERT INTO administration
        VALUES (
            null,
            #(driverId),
            #(personId),
            #(companyId),
            #(birthdate),
            #(email),
            #(password),
            #(picture),
            #(status),
            #{txHost},
            #{txUserId},
            #{txDate}
        )
    </insert>
</mapper>
