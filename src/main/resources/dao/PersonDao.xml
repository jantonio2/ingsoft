<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="bo.ucb.edu.ingsoft.dao.PersonDao">
    <insert id="createPerson" parameterType="bo.ucb.edu.ingsoft.modelo.Person">
        INSERT INTO person VALUES (
            null, #{firstName}, #{firstSurname}, #{secondSurname}, #{ci},
            #{phone}, 1, #{transaction.txId}, #{transaction.txHost}, #{transaction.txUserId},
            #{transaction.txDate}
        )
    </insert>
    <update id="updatePerson" parameterType="bo.ucb.edu.ingsoft.modelo.Person">
        UPDATE person SET
            first_name=#{firstName},
            first_surname=#{firstSurname},
            second_surname=#{secondSurname},
            ci=#{ci},
            phone=#{phone},
            status=2
            WHERE person_id= #{personId}
    </update>
    <select id="idPersonUser" resultType="java.lang.Integer">
        SELECT
            person_id
        FROM user
        WHERE user_id=#{userId}
    </select>

</mapper>