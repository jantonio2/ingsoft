<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="bo.ucb.edu.ingsoft.dao.UserDao">
    <insert id="createPerson" parameterType="bo.ucb.edu.ingsoft.modelo.Person">
        INSERT INTO person VALUES (
            null, #{firstName}, #{firstSurname}, #{secondSurname}, #{ci},
            1, #{phone}, #{transaction.txId}, #{transaction.txHost}, #{transaction.txUserId},
            #{transaction.txDate}
        )
    </insert>
    <insert id="createUser" parameterType="bo.ucb.edu.ingsoft.modelo.User">
        INSERT INTO user VALUES (
            null, #{personId}, #{birthDate}, #{email}, #{password},
            null, 1, #{transaction.txId}, #{transaction.txHost}, #{transaction.txUserId},
            #{transaction.txDate}
        )
    </insert>
    <insert id="createAddress" parameterType="bo.ucb.edu.ingsoft.modelo.Address">
        INSERT INTO address VALUES (
            null, null , #{userId}, null, null,
            null, null, #{street}, #{zone}, #{city},
            #{country}, 1, #{transaction.txId}, #{transaction.txHost}, #{transaction.txUserId},
            #{transaction.txDate}
        )
    </insert>
</mapper>